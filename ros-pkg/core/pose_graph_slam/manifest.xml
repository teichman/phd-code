<package>
  <description brief="pose_graph_slam">

     pose_graph_slam

  </description>
  <author>teichman</author>
  <license>BSD</license>
  <review status="unreviewed" notes=""/>
  <url>http://ros.org/wiki/pose_graph_slam</url>

  <!-- pcl_bleeding has to come first so that it gets used rather than the ros version -->
  <depend package="pcl_bleeding"/>
  <depend package="g2o"/>
  <depend package="rosconsole"/>
  <depend package="serializable"/>
  <depend package="eigen_extensions"/>

  <export>
    <cpp cflags="-I${prefix}/include"
	 lflags="-Wl,-rpath,${prefix}/lib -L${prefix}/lib -lpose_graph_slam" />
  </export>
  
</package>


